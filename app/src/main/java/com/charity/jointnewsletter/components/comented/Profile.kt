package com.charity.jointnewsletter.components.comented
/*
import android.content.Context
import android.content.Intent
import android.os.Bundle
import androidx.activity.compose.setContent
import androidx.appcompat.app.AppCompatActivity
import com.charity.jointnewsletter.ui.theme.JointNewsletterTheme


class ProfileActivity : AppCompatActivity() {

    private val article: Article by lazy {
        intent?.getSerializableExtra(ARTICLE_ID) as Article
    }

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
                JointNewsletterTheme {
                ProfileScreen(article)
            }
        }
    }

    companion object {
        private const val ARTICLE_ID = "puppy_id"
        fun newIntent(context: Context, article: Article) =
            Intent(context, ProfileActivity::class.java).apply {
                putExtra(ARTICLE_ID, article)
            }
    }
}

private fun Intent.putExtra(articleId: String, article: Article) {

}
*/
