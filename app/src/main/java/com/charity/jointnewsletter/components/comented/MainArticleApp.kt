package com.charity.jointnewsletter.components.comented
/*
import android.annotation.SuppressLint
import android.os.Bundle
import androidx.activity.compose.setContent
import androidx.appcompat.app.AppCompatActivity
import androidx.compose.material3.Scaffold
import androidx.compose.runtime.Composable
import androidx.compose.ui.tooling.preview.Preview

import com.charity.jointnewsletter.ui.theme.JointNewsletterTheme

class MainActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContent {
            JointNewsletterTheme {
                MyArticleApp {
                    startActivity(Profile.newIntent(this, it))
                }
            }
        }
    }
}

@SuppressLint("UnusedMaterial3ScaffoldPaddingParameter")
@Composable
fun MyArticleApp(navigateToProfile: (Article) -> Unit) {
    Scaffold(
        content = {
            ArticleHomeContent(navigateToProfile = navigateToProfile)
        }
    )
}

@Preview("Light Theme", widthDp = 360, heightDp = 640)
@Composable
fun LightPreview() {
    JointNewsletterTheme {
        MyArticleApp { }
    }
}

@Preview("Dark Theme", widthDp = 360, heightDp = 640)
@Composable
fun DarkPreview() {
    JointNewsletterTheme(darkTheme = true) {
        MyArticleApp { }
    }
}
*/
